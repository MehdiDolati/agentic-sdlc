openapi: 3.0.0
info:
  title: API - Add search to notes list
  version: '1.0.0'
paths:
  /api/notes:
    get:
      summary: List notes (searchable)
      responses:
        '200':
          description: OK
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
# (Generated by MockLLM)
